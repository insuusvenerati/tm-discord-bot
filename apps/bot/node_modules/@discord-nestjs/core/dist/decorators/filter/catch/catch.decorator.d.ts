import { Type } from '@nestjs/common/interfaces';
/**
 * Catch decorator
 *
 * Takes list of the types of exceptions that the filter will work with
 */
export declare function Catch(...exceptions: Type[]): ClassDecorator;
