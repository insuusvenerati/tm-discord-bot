import { EventType } from '../../../definitions/types/event.type';
/**
 * Once handle event decorator
 *
 * Subscribe only once to Discord event
 */
export declare function Once(event: EventType): MethodDecorator;
