import { PipeType } from '../../definitions/types/pipe.type';
/**
 * UsePipes decorator
 *
 * Takes list of pipes that transform values in turn
 */
export declare function UsePipes(...pipes: PipeType[]): MethodDecorator & ClassDecorator;
