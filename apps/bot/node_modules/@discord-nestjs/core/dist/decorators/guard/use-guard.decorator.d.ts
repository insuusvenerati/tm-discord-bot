import { GuardType } from '../../definitions/types/guard.type';
/**
 * UseGuards decorator
 *
 * Takes list of guards that are used to filter user requests
 */
export declare function UseGuards(...guards: GuardType[]): MethodDecorator & ClassDecorator;
