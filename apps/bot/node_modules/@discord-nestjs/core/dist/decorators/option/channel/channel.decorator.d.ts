import { ChannelType } from 'discord.js';
/**
 * Channel decorator
 *
 * Sets the list of channel types that can be selected
 */
export declare function Channel(types: ChannelType[]): PropertyDecorator;
