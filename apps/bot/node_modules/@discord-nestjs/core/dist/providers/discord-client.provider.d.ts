import { Client, WebhookClient } from 'discord.js';
import { ClientService } from '../services/client.service';
export declare class DiscordClientProvider {
    private readonly discordClientService;
    constructor(discordClientService: ClientService);
    getClient(): Client;
    getWebhookClient(): WebhookClient;
}
