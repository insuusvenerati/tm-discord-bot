import { Client } from 'discord.js';
import { DiscordCommandProvider } from '../providers/discord-command.provider';
import { OptionService } from './option.service';
export declare class RegisterCommandService {
    private readonly discordCommandProvider;
    private readonly client;
    private readonly discordOptionService;
    private readonly logger;
    constructor(discordCommandProvider: DiscordCommandProvider, client: Client, discordOptionService: OptionService);
    register(): Promise<void>;
    private registerCommands;
    private dropMissingCommands;
    private splitByCommands;
    private setupCommands;
}
