import { ReflectMetadataProvider } from '../../providers/reflect-metadata.provider';
import { InstantiationService } from '../../services/instantiation.service';
import { OptionService } from '../../services/option.service';
import { MethodExplorer } from '../interfaces/method-explorer';
import { MethodExplorerOptions } from '../interfaces/method-explorer-options';
import { DiscordGuardOptions } from './discord-guard-options';
export declare class GuardExplorer implements MethodExplorer {
    private readonly metadataProvider;
    private readonly discordOptionService;
    private readonly instantiationService;
    private readonly cachedGuards;
    constructor(metadataProvider: ReflectMetadataProvider, discordOptionService: OptionService, instantiationService: InstantiationService);
    explore(options: MethodExplorerOptions): Promise<void>;
    applyGuard(options: DiscordGuardOptions): Promise<boolean>;
}
