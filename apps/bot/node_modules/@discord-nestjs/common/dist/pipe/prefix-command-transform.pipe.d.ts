import { ArgNumOptions, ArgRangeOptions, DiscordArgumentMetadata, DiscordPipeTransform, ReflectMetadataProvider } from '@discord-nestjs/core';
import { ClassTransformOptions } from 'class-transformer';
import { Message } from 'discord.js';
export declare class PrefixCommandTransformPipe implements DiscordPipeTransform {
    private readonly metadataProvider;
    private readonly classTransformerOptions?;
    constructor(metadataProvider: ReflectMetadataProvider, classTransformerOptions?: ClassTransformOptions);
    transform(message: Message, metadata: DiscordArgumentMetadata<'messageCreate'>): InstanceType<any>;
    protected getArgNumValue(inputPart: string[], item: ArgNumOptions): string;
    protected getArgRangeValue(inputPart: string[], item: ArgRangeOptions): string[];
}
