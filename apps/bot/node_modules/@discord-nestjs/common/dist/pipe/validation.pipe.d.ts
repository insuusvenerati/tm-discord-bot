import { DiscordPipeTransform } from '@discord-nestjs/core';
import { ValidatorOptions } from 'class-validator';
/**
 * Validates DTO with class-validator
 */
export declare class ValidationPipe implements DiscordPipeTransform {
    private readonly validatorOptions?;
    constructor(validatorOptions?: ValidatorOptions);
    transform(dtoInstance: object): Promise<any>;
}
